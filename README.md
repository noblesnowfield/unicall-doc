# unicall-doc

Unicall 文档站，基于 VitePress。

## 本地开发

```bash
pnpm install
pnpm docs:dev
```

## 构建

```bash
pnpm docs:build
```

文档内容按快速开始、接入方式、核心概念、API、消息格式、HTML 模板、Provider、测试贡献组织。Provider 示例与 `unicall` SDK 的测试脚本和配置示例保持一致。
